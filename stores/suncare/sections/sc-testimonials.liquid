{%- comment -%}
  Suncare testimonials — large two-line heading with accent markup, arrow controls,
  cards with rounded-square avatar, name, gold stars, quote.
{%- endcomment -%}
<section class="sct sct-{{ section.id }}" style="--sct-accent: {{ section.settings.accent_color }}; --sct-star: {{ section.settings.star_color }};">
  <div class="page-width">
    <div class="sct__head">
      <h2 class="sct__heading">{{ section.settings.heading }}</h2>
      <div class="sct__nav">
        <button type="button" class="sct__arrow" data-sct-prev aria-label="{{ 'sections.slideshow.previous_slide' | t | default: 'Previous review' }}">
          <svg viewBox="0 0 34 12" fill="none" aria-hidden="true"><path d="M8 1 2 6l6 5M2 6h32" stroke="currentColor" stroke-width="1.4"/></svg>
        </button>
        <button type="button" class="sct__arrow" data-sct-next aria-label="{{ 'sections.slideshow.next_slide' | t | default: 'Next review' }}">
          <svg viewBox="0 0 34 12" fill="none" aria-hidden="true"><path d="M26 1l6 5-6 5M32 6H0" stroke="currentColor" stroke-width="1.4"/></svg>
        </button>
      </div>
    </div>
    <div class="sct__grid" data-sct-track>
      {%- for block in section.blocks -%}
        <div class="sct__card" {{ block.shopify_attributes }}>
          <div class="sct__top">
            {%- if block.settings.avatar != blank -%}
              {{ block.settings.avatar | image_url: width: 160 | image_tag: class: 'sct__avatar', loading: 'lazy', alt: block.settings.name }}
            {%- else -%}
              <img class="sct__avatar" src="{% if forloop.index == 2 %}{{ 'sc-ava-2.jpg' | asset_url }}{% else %}{{ 'sc-ava-1.jpg' | asset_url }}{% endif %}" alt="{{ block.settings.name | escape }}" width="80" height="80" loading="lazy">
            {%- endif -%}
            <div>
              <p class="sct__name">{{ block.settings.name | escape }}</p>
              <p class="sct__stars" aria-label="5 stars">★★★★★</p>
            </div>
          </div>
          <p class="sct__quote">“{{ block.settings.quote | escape }}”</p>
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>
{% style %}
  .sct-{{ section.id }} { background: var(--color-background); padding-block: clamp(2.5rem, 6vh, 4rem); }
  .sct-{{ section.id }} .sct__head { display: flex; align-items: flex-end; justify-content: space-between; gap: 1.5rem; margin-bottom: 1.6rem; }
  .sct-{{ section.id }} .sct__heading { font-family: var(--font-heading); font-size: clamp(1.9rem, 3.4vw, 2.7rem); font-weight: 500; color: var(--color-heading); line-height: 1.2; }
  .sct-{{ section.id }} .sct__heading em { font-style: normal; color: var(--sct-accent); }
  .sct-{{ section.id }} .sct__nav { display: flex; gap: 0.8rem; }
  .sct-{{ section.id }} .sct__arrow {
    width: 3rem; height: 3rem; border-radius: 50%; border: none; cursor: pointer;
    background: {{ section.settings.avatar_bg }}; color: {{ section.settings.avatar_ink }}; display: grid; place-items: center;
  }
  .sct-{{ section.id }} .sct__arrow svg { width: 1.6rem; height: 0.6rem; }
  .sct-{{ section.id }} .sct__grid {
    display: grid; grid-template-columns: repeat({{ section.blocks.size | at_most: 2 }}, 1fr); gap: clamp(1rem, 2.4vw, 1.8rem);
  }
  .sct-{{ section.id }} .sct__card {
    background: #dcedd8; border-radius: 14px; padding: 1.3rem 1.4rem;
    display: flex; flex-direction: column; gap: 0.9rem;
  }
  .sct-{{ section.id }} .sct__top { display: flex; align-items: center; gap: 0.9rem; }
  .sct-{{ section.id }} .sct__avatar { width: 4.4rem; height: 4.4rem; border-radius: 12px; object-fit: cover; }
  .sct-{{ section.id }} .sct__name { font-weight: 700; font-size: 0.95rem; color: var(--color-heading); }
  .sct-{{ section.id }} .sct__stars { color: var(--sct-star); font-size: 0.78rem; letter-spacing: 0.14em; margin-top: 0.2rem; }
  .sct-{{ section.id }} .sct__quote { font-size: 0.84rem; line-height: 1.65; color: var(--color-text-muted); }
  @media (max-width: 760px) {
    .sct-{{ section.id }} .sct__grid { grid-template-columns: 1fr; }
    .sct-{{ section.id }} .sct__nav { display: none; }
  }
{% endstyle %}
<script>
  (function () {
    var section = document.querySelector('.sct-{{ section.id }}');
    if (!section) return;
    var track = section.querySelector('[data-sct-track]');
    var step = function () { return (track.firstElementChild ? track.firstElementChild.offsetWidth : 320) + 24; };
    section.querySelector('[data-sct-prev]').addEventListener('click', function () { track.scrollBy({ left: -step(), behavior: 'smooth' }); });
    section.querySelector('[data-sct-next]').addEventListener('click', function () { track.scrollBy({ left: step(), behavior: 'smooth' }); });
  })();
</script>
{% style %}
  /* phase 2 enrichment: every rule is merchant-gated; defaults change nothing */
  {%- if section.settings.enr_heading_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-heading: {{ section.settings.enr_heading_color }}; }
  #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3, #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} [class*="__title"] { color: var(--enr-heading); }
  {%- endif -%}
  {%- if section.settings.enr_text_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-text: {{ section.settings.enr_text_color }}; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"], #shopify-section-{{ section.id }} [class*="__sub"], #shopify-section-{{ section.id }} [class*="__desc"] { color: var(--enr-text); }
  {%- endif -%}
  {%- if section.settings.enr_accent_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-accent: {{ section.settings.enr_accent_color }}; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"], #shopify-section-{{ section.id }} [class*="__kicker"] { color: var(--enr-accent); }
  {%- endif -%}
  {%- if section.settings.enr_card_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-card: {{ section.settings.enr_card_bg }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { background-color: var(--enr-card); }
  {%- endif -%}
  {%- if section.settings.enr_border_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-border: {{ section.settings.enr_border_color }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { border-color: var(--enr-border); }
  {%- endif -%}
  {%- if section.settings.enr_muted_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-muted: {{ section.settings.enr_muted_color }}; }
  #shopify-section-{{ section.id }} small, #shopify-section-{{ section.id }} [class*="__meta"], #shopify-section-{{ section.id }} [class*="__note"], #shopify-section-{{ section.id }} [class*="__caption"] { color: var(--enr-muted); }
  {%- endif -%}
  {%- if section.settings.enr_fallback_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-fallback: {{ section.settings.enr_fallback_bg }}; }
  #shopify-section-{{ section.id }} [class*="__placeholder"], #shopify-section-{{ section.id }} .placeholder-svg { background-color: var(--enr-fallback); }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'narrow' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: 46rem; }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'wide' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: none; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'left' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: left; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'center' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: center; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'right' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: right; }
  {%- endif -%}
  {%- if section.settings.enr_radius > 0 -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__media"], #shopify-section-{{ section.id }} img { border-radius: {{ section.settings.enr_radius }}px; }
  {%- endif -%}
  {%- if section.settings.enr_border_width != '0' -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"] { border: {{ section.settings.enr_border_width }}px solid var(--color-border, currentColor); }
  {%- endif -%}
  {%- if section.settings.enable_custom_sizes -%}
  #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3 { font-size: {{ section.settings.enr_title_size }}px; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"] { font-size: {{ section.settings.enr_body_size }}px; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"] { font-size: {{ section.settings.enr_eyebrow_size }}px; }
  #shopify-section-{{ section.id }} [class*="__icon"] svg, #shopify-section-{{ section.id }} svg.icon { width: {{ section.settings.enr_icon_size }}px; height: {{ section.settings.enr_icon_size }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_spacing -%}
  #shopify-section-{{ section.id }} > section, #shopify-section-{{ section.id }} > div { padding-block: {{ section.settings.enr_space_top }}px {{ section.settings.enr_space_bottom }}px; }
  #shopify-section-{{ section.id }} [class*="__grid"], #shopify-section-{{ section.id }} [class*="__row"], #shopify-section-{{ section.id }} [class*="__cards"], #shopify-section-{{ section.id }} [class*="__list"] { gap: {{ section.settings.enr_gap }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_media -%}
  #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height }}px; width: 100%; object-fit: cover; }
  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height_mobile }}px; }
  }
  {%- endif -%}
{% endstyle %}
{% schema %}
{
  "name": "SC Testimonials",
  "tag": "section",
  "max_blocks": 4,
  "settings": [
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading (inline <em> for green accent)",
      "default": "Our <em>Beauty</em><br><em>Community</em> Speaks"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent color",
      "default": "#7ba888"
    },
    {
      "type": "color",
      "id": "star_color",
      "label": "Star color",
      "default": "#e8a33d"
    },
    {
      "type": "color",
      "id": "avatar_bg",
      "label": "Avatar circle background",
      "default": "#e3f0dc"
    },
    {
      "type": "color",
      "id": "avatar_ink",
      "label": "Avatar initials color",
      "default": "#4c7a5d"
    },
    {
      "type": "header",
      "content": "Enriched colors (blank = as designed)"
    },
    {
      "type": "color",
      "id": "enr_heading_color",
      "label": "Heading color"
    },
    {
      "type": "color",
      "id": "enr_text_color",
      "label": "Body text color"
    },
    {
      "type": "color",
      "id": "enr_accent_color",
      "label": "Accent / eyebrow color"
    },
    {
      "type": "color",
      "id": "enr_card_bg",
      "label": "Card background"
    },
    {
      "type": "color",
      "id": "enr_border_color",
      "label": "Card border color"
    },
    {
      "type": "color",
      "id": "enr_muted_color",
      "label": "Muted text color"
    },
    {
      "type": "color",
      "id": "enr_fallback_bg",
      "label": "Image fallback background"
    },
    {
      "type": "header",
      "content": "Enriched layout"
    },
    {
      "type": "select",
      "id": "enr_layout_width",
      "label": "Content width",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "narrow",
          "label": "Narrow"
        },
        {
          "value": "wide",
          "label": "Wide"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "select",
      "id": "enr_layout_align",
      "label": "Intro alignment",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "header",
      "content": "Enriched shape"
    },
    {
      "type": "range",
      "id": "enr_radius",
      "label": "Card corner radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "enr_border_width",
      "label": "Card border width",
      "options": [
        {
          "value": "0",
          "label": "None"
        },
        {
          "value": "1",
          "label": "1 px"
        },
        {
          "value": "2",
          "label": "2 px"
        },
        {
          "value": "3",
          "label": "3 px"
        },
        {
          "value": "4",
          "label": "4 px"
        }
      ],
      "default": "0"
    },
    {
      "type": "header",
      "content": "Enriched sizes (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_sizes",
      "label": "Enable custom sizes",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_title_size",
      "label": "Title size",
      "min": 16,
      "max": 96,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "enr_body_size",
      "label": "Body size",
      "min": 10,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "enr_eyebrow_size",
      "label": "Eyebrow size",
      "min": 10,
      "max": 28,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "enr_icon_size",
      "label": "Icon size",
      "min": 12,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Enriched spacing (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_spacing",
      "label": "Enable custom spacing",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_space_top",
      "label": "Space top",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_space_bottom",
      "label": "Space bottom",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_gap",
      "label": "Grid gap",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Enriched media (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_media",
      "label": "Enable custom media heights",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_media_height",
      "label": "Media height (desktop)",
      "min": 380,
      "max": 580,
      "step": 2,
      "unit": "px",
      "default": 480
    },
    {
      "type": "range",
      "id": "enr_media_height_mobile",
      "label": "Media height (mobile)",
      "min": 300,
      "max": 500,
      "step": 2,
      "unit": "px",
      "default": 400
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "image_picker",
          "id": "avatar",
          "label": "Avatar"
        },
        {
          "type": "text",
          "id": "name",
          "label": "Name",
          "default": "Sarah Mitchell"
        },
        {
          "type": "textarea",
          "id": "quote",
          "label": "Quote",
          "default": "This cream is a game-changer for summer in Bangalore! My skin feels amazing and looks so much brighter."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "SC Testimonials",
      "blocks": [
        {
          "type": "testimonial"
        },
        {
          "type": "testimonial"
        }
      ]
    }
  ]
}
  {% endschema %}
