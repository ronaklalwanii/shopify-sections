{%- comment -%} Section Library: injected global assets for store "pyko" {%- endcomment -%}
{{ 'pyko--base.css' | asset_url | stylesheet_tag }}
{{ 'pyko--components.css' | asset_url | stylesheet_tag }}
{% render 'pyko--layout-tokens' %}
{{ 'pyko--global.js' | asset_url | script_tag }}
{{ 'pyko--wishlist.js' | asset_url | script_tag }}
{%- comment -%}
  Testimonial feature — large serif quote with author, prev/next arrows and a
  reviewer card beside a rounded image (Pyko composition).
{%- endcomment -%}
{%- capture section_colors_style -%}
  {%- if section.settings.colors_background != blank or section.settings.colors_text != blank -%}
    {%- assign override_bg = section.settings.colors_background | default: settings.colors_background -%}
    {%- assign override_fg = section.settings.colors_text | default: settings.colors_text -%}
    style="--color-background: {{ override_bg }}; --color-surface: {{ override_bg | color_lighten: 3 }}; --color-border: {{ override_bg | color_darken: 9 | color_modify: 'alpha', 0.85 }}; --color-foreground: {{ override_fg }}; --color-heading: {{ override_fg }}; --color-text-muted: {{ override_fg | color_modify: 'alpha', 0.62 }};"
  {%- endif -%}
{%- endcapture -%}
<section {{ section_colors_style }} class="section testi testi-{{ section.id }}" style="--section-space: {{ section.settings.padding | default: 80 }}px;">
  <div class="page-width testi__grid">
    <div class="testi__content">
      <span class="testi__mark" aria-hidden="true">&ldquo;</span>

      <div class="testi__quotes" data-testi>
        {%- for block in section.blocks -%}
          <div class="testi__quote-group{% if forloop.first %} is-active{% endif %}" data-testi-quote {{ block.shopify_attributes }}>
            <blockquote class="testi__quote">&ldquo;{{ block.settings.text | escape }}&rdquo;</blockquote>
            {%- if block.settings.author != blank -%}
              <p class="testi__author">- {{ block.settings.author | escape }}</p>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </div>

      <div class="testi__nav">
        <button type="button" class="testi__arrow testi__arrow--prev" data-testi-prev aria-label="{{ 'general.slider.previous' | t }}">
          {% render 'pyko--icon', name: 'chevron-left', size: 12 %}
        </button>
        <button type="button" class="testi__arrow testi__arrow--next" data-testi-next aria-label="{{ 'general.slider.next' | t }}">
          {% render 'pyko--icon', name: 'chevron-right', size: 12 %}
        </button>
      </div>

      {%- if section.settings.person_name != blank -%}
        <div class="testi__person">
          <span class="testi__person-avatar">
            {%- if section.settings.person_avatar != blank -%}
              {{ section.settings.person_avatar | image_url: width: 160 | image_tag: loading: 'lazy', alt: section.settings.person_name }}
            {%- else -%}
              {{ 'avatar-2' | placeholder_svg_tag: 'placeholder-svg' }}
            {%- endif -%}
          </span>
          <span class="testi__person-copy">
            <span class="testi__person-name">{{ section.settings.person_name | escape }}</span>
            {%- if section.settings.person_role != blank -%}
              <span class="testi__person-role">{{ section.settings.person_role | escape }}</span>
            {%- endif -%}
          </span>
        </div>
      {%- endif -%}
    </div>

    <div class="testi__media">
      {%- if section.settings.show_image and section.settings.image != blank -%}
        {{ section.settings.image | image_url: width: 1200 | image_tag: loading: 'lazy', widths: '480, 720, 960, 1200', sizes: '(min-width: 990px) 50vw, 100vw', alt: section.settings.image.alt }}
      {%- else -%}
        {{ 'lifestyle-2' | placeholder_svg_tag: 'placeholder-svg' }}
      {%- endif -%}
    </div>
  </div>
</section>

<style>
  .testi-{{ section.id }} { --testi-ink: {{ section.settings.ink_color }}; }

  .testi-{{ section.id }} .testi__grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: stretch;
  }
  .testi-{{ section.id }} .testi__content { display: flex; flex-direction: column; padding-block: clamp(0.5rem, 2vh, 1.5rem); }
  .testi-{{ section.id }} .testi__mark {
    font-family: var(--font-heading); font-weight: 700;
    font-size: 3.4rem; line-height: 1;
    display: block; margin-bottom: clamp(1rem, 3vh, 2rem);
  }
  .testi-{{ section.id }} .testi__quotes { flex: 1; }
  .testi-{{ section.id }} .testi__quote-group { display: none; }
  .testi-{{ section.id }} .testi__quote-group.is-active { display: block; }
  .testi-{{ section.id }} .testi__quote {
    font-family: var(--font-heading);
    font-size: clamp(1.25rem, 1.9vw, 1.7rem);
    line-height: 1.5;
    margin: 0 0 1.2rem;
    max-width: 30rem;
  }
  .testi-{{ section.id }} .testi__author { font-size: 0.95rem; color: var(--color-text-muted); margin: 0; }
  .testi-{{ section.id }} .testi__nav { display: flex; flex-direction: column; align-items: flex-start; gap: 0.4rem; margin-top: clamp(1.8rem, 5vh, 3.5rem); }
  .testi-{{ section.id }} .testi__arrow {
    width: 2.2rem; height: 1.9rem; border-radius: 999px; cursor: pointer;
    display: inline-flex; align-items: center; justify-content: center;
    border: 1px solid var(--color-border); background: transparent; color: var(--color-heading);
  }
  .testi-{{ section.id }} .testi__arrow--next { background: var(--testi-ink); color: #fff; border-color: var(--testi-ink); }
  .testi-{{ section.id }} .testi__person { display: flex; align-items: center; gap: 0.8rem; margin-top: clamp(1.5rem, 4vh, 2.5rem); }
  .testi-{{ section.id }} .testi__person-avatar {
    width: 4.2rem; height: 3.6rem; border-radius: 0.7rem; overflow: hidden;
    background: var(--color-background-secondary); display: inline-flex; flex-shrink: 0;
  }
  .testi-{{ section.id }} .testi__person-avatar img { width: 100%; height: 100%; object-fit: cover; }
  .testi-{{ section.id }} .testi__person-copy { display: flex; flex-direction: column; gap: 0.05rem; }
  .testi-{{ section.id }} .testi__person-name { font-size: 0.88rem; font-weight: 700; }
  .testi-{{ section.id }} .testi__person-role { font-size: 0.75rem; color: var(--color-text-muted); }
  .testi-{{ section.id }} .testi__media { border-radius: 1.5rem; overflow: hidden; background: var(--color-background-secondary); }
  .testi-{{ section.id }} .testi__media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  @media (max-width: 989px) {
    .testi-{{ section.id }} .testi__grid { grid-template-columns: 1fr; }
    .testi-{{ section.id }} .testi__media img { aspect-ratio: 4 / 3.4; }
  }
</style>

<script>
  (function () {
    var init = function () {
      var root = document.getElementById('shopify-section-{{ section.id }}');
      if (!root) return;
      root.querySelectorAll('[data-testi]').forEach(function (group) {
        var quotes = group.querySelectorAll('[data-testi-quote]');
        if (quotes.length < 2) return;
        var section = group.closest('.testi');
        var index = 0;
        var show = function (i) {
          index = (i + quotes.length) % quotes.length;
          quotes.forEach(function (q, n) { q.classList.toggle('is-active', n === index); });
        };
        var prev = section && section.querySelector('[data-testi-prev]');
        var next = section && section.querySelector('[data-testi-next]');
        if (prev) prev.addEventListener('click', function () { show(index - 1); });
        if (next) next.addEventListener('click', function () { show(index + 1); });
      });
    };
    init();
    document.addEventListener('shopify:section:load', init);
  })();
</script>

{% style %}
  /* phase 2 enrichment: every rule is merchant-gated; defaults change nothing */
  {%- if section.settings.enr_heading_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-heading: {{ section.settings.enr_heading_color }}; }
  #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3, #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} [class*="__title"] { color: var(--enr-heading); }
  {%- endif -%}
  {%- if section.settings.enr_text_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-text: {{ section.settings.enr_text_color }}; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"], #shopify-section-{{ section.id }} [class*="__sub"], #shopify-section-{{ section.id }} [class*="__desc"] { color: var(--enr-text); }
  {%- endif -%}
  {%- if section.settings.enr_accent_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-accent: {{ section.settings.enr_accent_color }}; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"], #shopify-section-{{ section.id }} [class*="__kicker"] { color: var(--enr-accent); }
  {%- endif -%}
  {%- if section.settings.enr_card_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-card: {{ section.settings.enr_card_bg }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { background-color: var(--enr-card); }
  {%- endif -%}
  {%- if section.settings.enr_border_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-border: {{ section.settings.enr_border_color }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { border-color: var(--enr-border); }
  {%- endif -%}
  {%- if section.settings.enr_muted_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-muted: {{ section.settings.enr_muted_color }}; }
  #shopify-section-{{ section.id }} small, #shopify-section-{{ section.id }} [class*="__meta"], #shopify-section-{{ section.id }} [class*="__note"], #shopify-section-{{ section.id }} [class*="__caption"] { color: var(--enr-muted); }
  {%- endif -%}
  {%- if section.settings.enr_fallback_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-fallback: {{ section.settings.enr_fallback_bg }}; }
  #shopify-section-{{ section.id }} [class*="__placeholder"], #shopify-section-{{ section.id }} .placeholder-svg { background-color: var(--enr-fallback); }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'narrow' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: 46rem; }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'wide' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: none; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'left' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: left; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'center' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: center; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'right' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: right; }
  {%- endif -%}
  {%- if section.settings.enr_radius > 0 -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__media"], #shopify-section-{{ section.id }} img { border-radius: {{ section.settings.enr_radius }}px; }
  {%- endif -%}
  {%- if section.settings.enr_border_width != '0' -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"] { border: {{ section.settings.enr_border_width }}px solid var(--color-border, currentColor); }
  {%- endif -%}
  {%- if section.settings.enable_custom_sizes -%}
  #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3 { font-size: {{ section.settings.enr_title_size }}px; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"] { font-size: {{ section.settings.enr_body_size }}px; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"] { font-size: {{ section.settings.enr_eyebrow_size }}px; }
  #shopify-section-{{ section.id }} [class*="__icon"] svg, #shopify-section-{{ section.id }} svg.icon { width: {{ section.settings.enr_icon_size }}px; height: {{ section.settings.enr_icon_size }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_spacing -%}
  #shopify-section-{{ section.id }} > section, #shopify-section-{{ section.id }} > div { padding-block: {{ section.settings.enr_space_top }}px {{ section.settings.enr_space_bottom }}px; }
  #shopify-section-{{ section.id }} [class*="__grid"], #shopify-section-{{ section.id }} [class*="__row"], #shopify-section-{{ section.id }} [class*="__cards"], #shopify-section-{{ section.id }} [class*="__list"] { gap: {{ section.settings.enr_gap }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_media -%}
  #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height }}px; width: 100%; object-fit: cover; }
  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height_mobile }}px; }
  }
  {%- endif -%}
{% endstyle %}
{% schema %}
{
  "name": "Testimonial feature",
  "max_blocks": 5,
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image (right column)"
    },
    {
      "type": "image_picker",
      "id": "person_avatar",
      "label": "Reviewer avatar"
    },
    {
      "type": "text",
      "id": "person_name",
      "label": "Reviewer name",
      "default": "Eleanor Pena"
    },
    {
      "type": "text",
      "id": "person_role",
      "label": "Reviewer role",
      "default": "President of Sales"
    },
    {
      "type": "range",
      "id": "padding",
      "label": "Vertical padding",
      "min": 24,
      "max": 140,
      "step": 2,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "colors_background",
      "label": "Background override",
      "info": "Leave empty to use the theme colors"
    },
    {
      "type": "color",
      "id": "colors_text",
      "label": "Text color override"
    },
    {
      "type": "color",
      "id": "ink_color",
      "label": "Arrow ink color",
      "default": "#1c1a16"
    },
    {
      "type": "header",
      "content": "Enriched colors (blank = as designed)"
    },
    {
      "type": "color",
      "id": "enr_heading_color",
      "label": "Heading color"
    },
    {
      "type": "color",
      "id": "enr_text_color",
      "label": "Body text color"
    },
    {
      "type": "color",
      "id": "enr_accent_color",
      "label": "Accent / eyebrow color"
    },
    {
      "type": "color",
      "id": "enr_card_bg",
      "label": "Card background"
    },
    {
      "type": "color",
      "id": "enr_border_color",
      "label": "Card border color"
    },
    {
      "type": "color",
      "id": "enr_muted_color",
      "label": "Muted text color"
    },
    {
      "type": "color",
      "id": "enr_fallback_bg",
      "label": "Image fallback background"
    },
    {
      "type": "header",
      "content": "Enriched layout"
    },
    {
      "type": "select",
      "id": "enr_layout_width",
      "label": "Content width",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "narrow",
          "label": "Narrow"
        },
        {
          "value": "wide",
          "label": "Wide"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "select",
      "id": "enr_layout_align",
      "label": "Intro alignment",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "header",
      "content": "Enriched shape"
    },
    {
      "type": "range",
      "id": "enr_radius",
      "label": "Card corner radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "enr_border_width",
      "label": "Card border width",
      "options": [
        {
          "value": "0",
          "label": "None"
        },
        {
          "value": "1",
          "label": "1 px"
        },
        {
          "value": "2",
          "label": "2 px"
        },
        {
          "value": "3",
          "label": "3 px"
        },
        {
          "value": "4",
          "label": "4 px"
        }
      ],
      "default": "0"
    },
    {
      "type": "header",
      "content": "Enriched sizes (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_sizes",
      "label": "Enable custom sizes",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_title_size",
      "label": "Title size",
      "min": 16,
      "max": 96,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "enr_body_size",
      "label": "Body size",
      "min": 10,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "enr_eyebrow_size",
      "label": "Eyebrow size",
      "min": 10,
      "max": 28,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "enr_icon_size",
      "label": "Icon size",
      "min": 12,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Enriched spacing (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_spacing",
      "label": "Enable custom spacing",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_space_top",
      "label": "Space top",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_space_bottom",
      "label": "Space bottom",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_gap",
      "label": "Grid gap",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Enriched media (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_media",
      "label": "Enable custom media heights",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_media_height",
      "label": "Media height (desktop)",
      "min": 380,
      "max": 580,
      "step": 2,
      "unit": "px",
      "default": 480
    },
    {
      "type": "range",
      "id": "enr_media_height_mobile",
      "label": "Media height (mobile)",
      "min": 300,
      "max": 500,
      "step": 2,
      "unit": "px",
      "default": 400
    },
    {
      "type": "header",
      "content": "Visibility"
    },
    {
      "type": "checkbox",
      "id": "show_image",
      "label": "Show image",
      "default": true
    }
  ],
  "blocks": [
    {
      "type": "quote",
      "name": "Quote",
      "settings": [
        {
          "type": "textarea",
          "id": "text",
          "label": "Quote",
          "default": "My skin feels softer, smoother, and noticeably more hydrated after just a week. It's now an essential part of my daily routine."
        },
        {
          "type": "text",
          "id": "author",
          "label": "Author",
          "default": "Sarah M."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Testimonial feature",
      "blocks": [
        {
          "type": "quote"
        }
      ]
    }
  ]
}
  {% endschema %}
