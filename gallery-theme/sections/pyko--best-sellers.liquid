{%- comment -%} Section Library: injected global assets for store "pyko" {%- endcomment -%}
{{ 'pyko--base.css' | asset_url | stylesheet_tag }}
{{ 'pyko--components.css' | asset_url | stylesheet_tag }}
{% render 'pyko--layout-tokens' %}
{{ 'pyko--global.js' | asset_url | script_tag }}
{{ 'pyko--wishlist.js' | asset_url | script_tag }}
{%- comment -%}
  Best sellers split — centered title, tall image beside an intro + product
  card carousel with pastel tiles and quick-add plus (Pyko composition).
{%- endcomment -%}
{%- liquid
  assign products = collections[section.settings.collection].products
  assign limit = section.settings.products_count
  assign pastels = section.settings.tile_colors | split: ','
-%}
{%- capture section_colors_style -%}
  {%- if section.settings.colors_background != blank or section.settings.colors_text != blank -%}
    {%- assign override_bg = section.settings.colors_background | default: settings.colors_background -%}
    {%- assign override_fg = section.settings.colors_text | default: settings.colors_text -%}
    style="--color-background: {{ override_bg }}; --color-surface: {{ override_bg | color_lighten: 3 }}; --color-border: {{ override_bg | color_darken: 9 | color_modify: 'alpha', 0.85 }}; --color-foreground: {{ override_fg }}; --color-heading: {{ override_fg }}; --color-text-muted: {{ override_fg | color_modify: 'alpha', 0.62 }};"
  {%- endif -%}
{%- endcapture -%}
<section {{ section_colors_style }} class="section bestsplit bestsplit-{{ section.id }}" style="--section-space: {{ section.settings.padding | default: 80 }}px;">
  <div class="page-width">
    {%- if section.settings.show_heading and section.settings.heading != blank -%}
      <h2 class="bestsplit__title h1">{{ section.settings.heading | escape | newline_to_br }}</h2>
    {%- endif -%}

    <div class="bestsplit__grid">
      <div class="bestsplit__media">
        {%- if section.settings.show_image and section.settings.image != blank -%}
          {{ section.settings.image | image_url: width: 1200 | image_tag: loading: 'lazy', widths: '480, 720, 960, 1200', sizes: '(min-width: 990px) 42vw, 100vw', alt: section.settings.image.alt }}
        {%- else -%}
          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
        {%- endif -%}
      </div>

      <div class="bestsplit__body">
        {%- if section.settings.intro != blank -%}
          <p class="bestsplit__intro">{{ section.settings.intro | escape }}</p>
        {%- endif -%}

        <div class="card-carousel bestsplit__carousel" data-carousel>
          <div class="bestsplit__nav">
            <span class="bestsplit__progress" aria-hidden="true"><span class="bestsplit__progress-fill" data-bestsplit-progress></span></span>
            <span class="bestsplit__arrows">
              <button type="button" class="bestsplit__arrow bestsplit__arrow--prev" data-carousel-prev aria-label="{{ 'general.slider.previous' | t }}">
                {% render 'pyko--icon', name: 'chevron-left', size: 12 %}
              </button>
              <button type="button" class="bestsplit__arrow bestsplit__arrow--next" data-carousel-next aria-label="{{ 'general.slider.next' | t }}">
                {% render 'pyko--icon', name: 'chevron-right', size: 12 %}
              </button>
            </span>
          </div>

          <div class="card-carousel__track bestsplit__track">
            {%- if products.size > 0 -%}
              {%- for product in products limit: limit -%}
                {% render 'pyko--product-card',
                  product: product,
                  show_subtitle: false,
                  show_price: true,
                  show_rating: false,
                  show_button: false,
                  image_ratio: section.settings.tile_ratio,
                  tile_bg: pastels[forloop.index0],
                  show_quick_plus: true,
                  price_align: 'split'
                %}
              {%- endfor -%}
            {%- else -%}
              {%- for i in (1..limit) -%}
                {%- assign ph = 'product-' | append: i -%}
                <div class="card product-card product-card--tile" style="--tile-bg: {{ pastels[forloop.index0] | default: '#f3d9d9' }};">
                  <div class="card__media ratio-{{ section.settings.tile_ratio }}">
                    {{ ph | placeholder_svg_tag: 'placeholder-svg' }}
                  </div>
                  <div class="card__content card__content--quick">
                    <div class="card__quick">
                      <span class="card__quick-info">
                        <h3 class="card__title">{{ 'onboarding.product_title' | t }}</h3>
                        <p class="card__price">{{ 4200 | money }}</p>
                      </span>
                      <span class="card__quick-plus">{% render 'pyko--icon', name: 'plus', size: 14 %}</span>
                    </div>
                  </div>
                </div>
              {%- endfor -%}
            {%- endif -%}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .bestsplit-{{ section.id }} .bestsplit__title { text-align: center; max-width: 40rem; margin: 0 auto clamp(2rem, 4.5vh, 3.2rem); font-weight: 500; }
  .bestsplit-{{ section.id }} .bestsplit__grid {
    display: grid;
    grid-template-columns: 0.92fr 1.08fr;
    gap: clamp(1.6rem, 3.5vw, 3.4rem);
    align-items: stretch;
  }
  .bestsplit-{{ section.id }} .bestsplit__media { border-radius: 1.5rem; overflow: hidden; background: var(--color-background-secondary); }
  .bestsplit-{{ section.id }} .bestsplit__media img { width: 100%; height: 100%; object-fit: cover; display: block; }
  .bestsplit-{{ section.id }} .bestsplit__body { display: flex; flex-direction: column; }
  .bestsplit-{{ section.id }} .bestsplit__intro { max-width: 26rem; font-size: 0.95rem; line-height: 1.6; margin: 0 0 clamp(1.5rem, 3vh, 2.4rem); }
  .bestsplit-{{ section.id }} .bestsplit__carousel { display: flex; flex-direction: column; }
  .bestsplit-{{ section.id }} .bestsplit__nav { display: flex; align-items: center; justify-content: space-between; gap: 1.2rem; margin-bottom: 1rem; }
  .bestsplit-{{ section.id }} .bestsplit__progress {
    flex: 1; max-width: 15rem; height: 2px; border-radius: 2px;
    background: var(--color-border); display: block; overflow: hidden;
  }
  .bestsplit-{{ section.id }} .bestsplit__progress-fill {
    display: block; width: 34%; height: 100%;
    background: var(--color-heading, #2f2a24);
    transition: width 0.25s ease;
  /* reduced-motion: kill all added motion */
  @media (prefers-reduced-motion: reduce) {
    .bestsplit-{{ section.id }},
    .bestsplit-{{ section.id }} * {
      animation: none !important;
      transition: none !important;
    }
  }
  }
  .bestsplit-{{ section.id }} .bestsplit__arrows { display: inline-flex; gap: 0.45rem; }
  .bestsplit-{{ section.id }} .bestsplit__arrow {
    width: 2.1rem; height: 1.8rem; border-radius: 999px; border: none; cursor: pointer;
    display: inline-flex; align-items: center; justify-content: center;
    background: var(--color-surface, #fff); color: var(--color-heading);
    border: 1px solid var(--color-border);
  }
  .bestsplit-{{ section.id }} .bestsplit__arrow--next { background: #1c1a16; color: #ffffff; border-color: #1c1a16; }
  .bestsplit-{{ section.id }} .bestsplit__track { --visible: 3; }
  .bestsplit-{{ section.id }} .bestsplit__track .product-card--tile { scroll-snap-align: start; }
  @media (max-width: 989px) {
    .bestsplit-{{ section.id }} .bestsplit__grid { grid-template-columns: 1fr; }
    .bestsplit-{{ section.id }} .bestsplit__media img { aspect-ratio: 4 / 4.4; }
    .bestsplit-{{ section.id }} .bestsplit__track { --visible: 2; }
  }
</style>

<script>
  (function () {
    var init = function () {
      var root = document.getElementById('shopify-section-{{ section.id }}');
      if (!root) return;
      root.querySelectorAll('[data-bestsplit-progress]').forEach(function (fill) {
        var carousel = fill.closest('[data-carousel]');
        var track = carousel && carousel.querySelector('.card-carousel__track');
        if (!track) return;
        var update = function () {
          var max = track.scrollWidth - track.clientWidth;
          var ratio = max > 0 ? track.scrollLeft / max : 0;
          var base = track.clientWidth / track.scrollWidth;
          fill.style.width = Math.max(0.12, base + (1 - base) * ratio) * 100 + '%';
        };
        track.addEventListener('scroll', update, { passive: true });
        window.addEventListener('resize', update);
        update();
      });
    };
    init();
    document.addEventListener('shopify:section:load', init);
  })();
</script>

{% style %}
  /* phase 2 enrichment: every rule is merchant-gated; defaults change nothing */
  {%- if section.settings.enr_heading_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-heading: {{ section.settings.enr_heading_color }}; }
  #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3, #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} [class*="__title"] { color: var(--enr-heading); }
  {%- endif -%}
  {%- if section.settings.enr_text_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-text: {{ section.settings.enr_text_color }}; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"], #shopify-section-{{ section.id }} [class*="__sub"], #shopify-section-{{ section.id }} [class*="__desc"] { color: var(--enr-text); }
  {%- endif -%}
  {%- if section.settings.enr_accent_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-accent: {{ section.settings.enr_accent_color }}; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"], #shopify-section-{{ section.id }} [class*="__kicker"] { color: var(--enr-accent); }
  {%- endif -%}
  {%- if section.settings.enr_card_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-card: {{ section.settings.enr_card_bg }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { background-color: var(--enr-card); }
  {%- endif -%}
  {%- if section.settings.enr_border_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-border: {{ section.settings.enr_border_color }}; }
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__box"] { border-color: var(--enr-border); }
  {%- endif -%}
  {%- if section.settings.enr_muted_color != blank -%}
  #shopify-section-{{ section.id }} { --enr-muted: {{ section.settings.enr_muted_color }}; }
  #shopify-section-{{ section.id }} small, #shopify-section-{{ section.id }} [class*="__meta"], #shopify-section-{{ section.id }} [class*="__note"], #shopify-section-{{ section.id }} [class*="__caption"] { color: var(--enr-muted); }
  {%- endif -%}
  {%- if section.settings.enr_fallback_bg != blank -%}
  #shopify-section-{{ section.id }} { --enr-fallback: {{ section.settings.enr_fallback_bg }}; }
  #shopify-section-{{ section.id }} [class*="__placeholder"], #shopify-section-{{ section.id }} .placeholder-svg { background-color: var(--enr-fallback); }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'narrow' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: 46rem; }
  {%- endif -%}
  {%- if section.settings.enr_layout_width == 'wide' -%}
  #shopify-section-{{ section.id }} .page-width { max-width: none; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'left' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: left; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'center' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: center; }
  {%- endif -%}
  {%- if section.settings.enr_layout_align == 'right' -%}
  #shopify-section-{{ section.id }} [class*="__head"], #shopify-section-{{ section.id }} [class*="__intro"] { text-align: right; }
  {%- endif -%}
  {%- if section.settings.enr_radius > 0 -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"], #shopify-section-{{ section.id }} [class*="__media"], #shopify-section-{{ section.id }} img { border-radius: {{ section.settings.enr_radius }}px; }
  {%- endif -%}
  {%- if section.settings.enr_border_width != '0' -%}
  #shopify-section-{{ section.id }} [class*="__card"], #shopify-section-{{ section.id }} [class*="__tile"], #shopify-section-{{ section.id }} [class*="__panel"] { border: {{ section.settings.enr_border_width }}px solid var(--color-border, currentColor); }
  {%- endif -%}
  {%- if section.settings.enable_custom_sizes -%}
  #shopify-section-{{ section.id }} [class*="__heading"], #shopify-section-{{ section.id }} h1, #shopify-section-{{ section.id }} h2, #shopify-section-{{ section.id }} h3 { font-size: {{ section.settings.enr_title_size }}px; }
  #shopify-section-{{ section.id }} p, #shopify-section-{{ section.id }} li, #shopify-section-{{ section.id }} [class*="__text"] { font-size: {{ section.settings.enr_body_size }}px; }
  #shopify-section-{{ section.id }} .eyebrow, #shopify-section-{{ section.id }} [class*="__eyebrow"] { font-size: {{ section.settings.enr_eyebrow_size }}px; }
  #shopify-section-{{ section.id }} [class*="__icon"] svg, #shopify-section-{{ section.id }} svg.icon { width: {{ section.settings.enr_icon_size }}px; height: {{ section.settings.enr_icon_size }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_spacing -%}
  #shopify-section-{{ section.id }} > section, #shopify-section-{{ section.id }} > div { padding-block: {{ section.settings.enr_space_top }}px {{ section.settings.enr_space_bottom }}px; }
  #shopify-section-{{ section.id }} [class*="__grid"], #shopify-section-{{ section.id }} [class*="__row"], #shopify-section-{{ section.id }} [class*="__cards"], #shopify-section-{{ section.id }} [class*="__list"] { gap: {{ section.settings.enr_gap }}px; }
  {%- endif -%}
  {%- if section.settings.enable_custom_media -%}
  #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height }}px; width: 100%; object-fit: cover; }
  @media (max-width: 749px) {
    #shopify-section-{{ section.id }} [class*="__media"] img, #shopify-section-{{ section.id }} [class*="__image"] img, #shopify-section-{{ section.id }} [class*="__photo"] img { max-height: {{ section.settings.enr_media_height_mobile }}px; }
  }
  {%- endif -%}
{% endstyle %}
{% schema %}
{
  "name": "Best sellers split",
  "settings": [
    {
      "type": "textarea",
      "id": "heading",
      "label": "Heading",
      "default": "Products That Are Among\nOur Best Sellers"
    },
    {
      "type": "text",
      "id": "intro",
      "label": "Intro text",
      "default": "Our Most-Loved Skincare Essentials Trusted By Thousands For Healthier, Glowing Skin."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image (left column)"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_count",
      "label": "Products to show",
      "min": 2,
      "max": 8,
      "step": 2,
      "default": 4
    },
    {
      "type": "text",
      "id": "tile_colors",
      "label": "Tile colors",
      "info": "Comma-separated hex colors cycled across product tiles.",
      "default": "#f3d9dc,#f7ead7,#f2edcb,#e0e8d5"
    },
    {
      "type": "select",
      "id": "tile_ratio",
      "label": "Tile image ratio",
      "options": [
        {
          "value": "portrait",
          "label": "Portrait (3:4)"
        },
        {
          "value": "tall",
          "label": "Tall (4:5)"
        },
        {
          "value": "square",
          "label": "Square (1:1)"
        }
      ],
      "default": "tall"
    },
    {
      "type": "range",
      "id": "padding",
      "label": "Vertical padding",
      "min": 24,
      "max": 140,
      "step": 2,
      "unit": "px",
      "default": 80
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "colors_background",
      "label": "Background override",
      "info": "Leave empty to use the theme colors"
    },
    {
      "type": "color",
      "id": "colors_text",
      "label": "Text color override"
    },
    {
      "type": "header",
      "content": "Enriched colors (blank = as designed)"
    },
    {
      "type": "color",
      "id": "enr_heading_color",
      "label": "Heading color"
    },
    {
      "type": "color",
      "id": "enr_text_color",
      "label": "Body text color"
    },
    {
      "type": "color",
      "id": "enr_accent_color",
      "label": "Accent / eyebrow color"
    },
    {
      "type": "color",
      "id": "enr_card_bg",
      "label": "Card background"
    },
    {
      "type": "color",
      "id": "enr_border_color",
      "label": "Card border color"
    },
    {
      "type": "color",
      "id": "enr_muted_color",
      "label": "Muted text color"
    },
    {
      "type": "color",
      "id": "enr_fallback_bg",
      "label": "Image fallback background"
    },
    {
      "type": "header",
      "content": "Enriched layout"
    },
    {
      "type": "select",
      "id": "enr_layout_width",
      "label": "Content width",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "narrow",
          "label": "Narrow"
        },
        {
          "value": "wide",
          "label": "Wide"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "select",
      "id": "enr_layout_align",
      "label": "Intro alignment",
      "options": [
        {
          "value": "as_designed",
          "label": "As designed"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "as_designed"
    },
    {
      "type": "header",
      "content": "Enriched shape"
    },
    {
      "type": "range",
      "id": "enr_radius",
      "label": "Card corner radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "default": 0
    },
    {
      "type": "select",
      "id": "enr_border_width",
      "label": "Card border width",
      "options": [
        {
          "value": "0",
          "label": "None"
        },
        {
          "value": "1",
          "label": "1 px"
        },
        {
          "value": "2",
          "label": "2 px"
        },
        {
          "value": "3",
          "label": "3 px"
        },
        {
          "value": "4",
          "label": "4 px"
        }
      ],
      "default": "0"
    },
    {
      "type": "header",
      "content": "Enriched sizes (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_sizes",
      "label": "Enable custom sizes",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_title_size",
      "label": "Title size",
      "min": 16,
      "max": 96,
      "step": 2,
      "unit": "px",
      "default": 32
    },
    {
      "type": "range",
      "id": "enr_body_size",
      "label": "Body size",
      "min": 10,
      "max": 32,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "enr_eyebrow_size",
      "label": "Eyebrow size",
      "min": 10,
      "max": 28,
      "step": 2,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "enr_icon_size",
      "label": "Icon size",
      "min": 12,
      "max": 64,
      "step": 2,
      "unit": "px",
      "default": 24
    },
    {
      "type": "header",
      "content": "Enriched spacing (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_spacing",
      "label": "Enable custom spacing",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_space_top",
      "label": "Space top",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_space_bottom",
      "label": "Space bottom",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "enr_gap",
      "label": "Grid gap",
      "min": 0,
      "max": 80,
      "step": 2,
      "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Enriched media (off = as designed)"
    },
    {
      "type": "checkbox",
      "id": "enable_custom_media",
      "label": "Enable custom media heights",
      "default": false
    },
    {
      "type": "range",
      "id": "enr_media_height",
      "label": "Media height (desktop)",
      "min": 380,
      "max": 580,
      "step": 2,
      "unit": "px",
      "default": 480
    },
    {
      "type": "range",
      "id": "enr_media_height_mobile",
      "label": "Media height (mobile)",
      "min": 300,
      "max": 500,
      "step": 2,
      "unit": "px",
      "default": 400
    },
    {
      "type": "header",
      "content": "Visibility"
    },
    {
      "type": "checkbox",
      "id": "show_heading",
      "label": "Show heading",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_image",
      "label": "Show image",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Best sellers split"
    }
  ]
}
  {% endschema %}
